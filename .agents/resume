#!/usr/bin/env bash
set -euo pipefail

# CP Platform — Orb resume
# Fast, idempotent reconnection checks.

if [ ! -d node_modules ]; then
  echo "node_modules missing — running npm ci..."
  npm ci
fi

echo "Resume complete."
